apiVersion: broker.amq.io/v2alpha2
kind: ActiveMQArtemisAddress
metadata:
  name: ${activemq.address}-address
spec:
  addressName: ${activemq.address}
  queueName: ${activemq.address}
  routingType: anycast
